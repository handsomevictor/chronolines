{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$BASH_COMMAND\" | grep -q 'build.py'; then python .claude/hooks/post-build.py; fi"
          }
        ]
      }
    ]
  }
}
